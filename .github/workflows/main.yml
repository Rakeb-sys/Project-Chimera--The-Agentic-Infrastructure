name: Chimera CI

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker
        run: docker build -t chimera .

      - name: Run Tests
        run: docker run chimera

<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="1000" height="700" viewBox="0 0 1000 700">
  <style>.title{font:bold 16px sans-serif;} pre{font:12px monospace;white-space:pre-wrap;}</style>
  <rect width="100%" height="100%" fill="#fff"/>
  <text x="20" y="30" class="title">System Component Diagram (Mermaid source)</text>
  <foreignObject x="20" y="50" width="960" height="620"><body xmlns="http://www.w3.org/1999/xhtml"><pre>
%% System Component Diagram
classDiagram
  class MCP {
    +submitTask()
    +getTaskStatus()
    +assignStep()
  }
  class MessageQueue {
    +enqueue()
    +dequeue()
  }
  class WorkerAgent {
    +run(task)
    +emitTelemetry()
  }
  class PolicyService {
    +evaluate(candidate)
  }
  class TelemetryCollector {
    +ingest(event)
    +query()
  }
  class ObjectStore {
    +put(blob)
    +get(blob)
  }
  MCP --> MessageQueue
  MessageQueue --> WorkerAgent
  WorkerAgent --> ObjectStore
  WorkerAgent --> TelemetryCollector
  MCP --> PolicyService
  PolicyService --> TelemetryCollector
  </pre></body></foreignObject>
</svg>